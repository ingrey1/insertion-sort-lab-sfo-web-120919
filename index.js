function findMinAndRemove(array){

    let max = array[0]
    let index = 0
    array.forEach(el => {
        if (el < max) {
            max = el
            index = i
        }    
    })

    array.splice(index, 1)


    

}

function insertionSort(array){

}
